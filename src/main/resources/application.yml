spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/promptgenius
    username: promptgenius
    password: promptgenius
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
